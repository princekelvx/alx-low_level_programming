#include "main.h"

/**
 * _sqrt_recursion - calculates the natural square root of a number
 * @n: number to derive the square root of
 * Return: derived square root
 */

int _sqrt_recursion(int n)
{
	if (n < 0)
		return (-1);

